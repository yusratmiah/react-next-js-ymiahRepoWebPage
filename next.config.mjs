/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: "/yusrat-next-react-project",
    images: {
      unoptimized: true,
    },
  };

export default nextConfig;
